const int LED_PIN = 23;
int TEMPS_ENCES = 250;
int TEMPS_APAGAT = 500;


void setup() {
  pinMode(LED_PIN, OUTPUT);
}


void loop() {
  digitalWrite(LED_PIN, HIGH);
  delay(TEMPS_ENCES);


  digitalWrite(LED_PIN, LOW);
  delay(TEMPS_APAGAT);
}
